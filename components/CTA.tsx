export function CTA() {
  return (
    <section id="build" className="text-center py-24">
      <div className="font-mono text-[12.5px] text-green tracking-wide mb-4">
        JOIN THE NEXT GENERATION OF ONCHAIN BUILDERS
      </div>
      <h2 className="font-display font-bold text-[30px] sm:text-5xl mb-4.5">
        Claim your .hood name.
      </h2>
      <p className="text-mute mb-8">Free to search. Testnet open now.</p>
      <a
        href="#names"
        className="inline-block text-[15.5px] font-semibold px-8 py-3.5 rounded-full bg-green text-[#04140A] hover:bg-[#12D815] transition"
      >
        Search a name
      </a>
    </section>
  );
}
